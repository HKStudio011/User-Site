@inject User_Site.Router.Router router;
@page "/"

<PageTitle>Home</PageTitle>

<div class="text-center space-y-6">
    <h1 class="text-4xl md:text-6xl font-extrabold tracking-tight
               text-transparent bg-clip-text bg-gradient-to-r from-pink-500 via-purple-500 to-indigo-500">
        Tailwind đang hoạt động?
    </h1>
    <p class="text-slate-300 max-w-md mx-auto">
        Nếu bạn thấy tiêu đề gradient, nền tối và các nút có style, Tailwind đã chạy ok.
    </p>

    <div class="flex items-center justify-center gap-3">
        <button class="px-5 py-2 rounded-lg bg-indigo-600 hover:bg-indigo-500 active:scale-95 transition">
            Nút chính
        </button>
        <button class="px-5 py-2 rounded-lg border border-slate-500 hover:border-slate-400 text-slate-200 transition">
            Nút phụ
        </button>
    </div>

    <div class="mx-auto w-64 h-2 bg-slate-700 rounded-full overflow-hidden">
        <div class="h-full w-1/2 bg-emerald-500 animate-pulse"></div>
    </div>
</div>

@code {

}
